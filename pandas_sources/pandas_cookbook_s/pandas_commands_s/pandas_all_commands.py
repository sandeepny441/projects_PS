# import pandas as pd 

# #chapter:5
# filter_cond = df.BASE_SALARY.between(100,200)
# abs_z_score = amzn_daily_return.sub(mean).abs().div(std) # symbols
# pcts = [abs_z_score.lt(i).mean() for i in range(1,4)] # less than 

this_list  = [10, 20]

for i, j in enumerate(a):
    print(i, j)
